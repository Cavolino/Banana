/* const condividiEvento = async (nomeEvento) => {
	try {
		var shareData = {
			title: 'Condividi Evento ' + nomeEvento + ' su Banana App',
			text: 'IdEvento: ' + nomeEvento,
			url: 'https://banana/events/' + nomeEvento
		}

		await navigator.share(shareData)
	} catch (err) {
		console.log('Error: ' + err);
	}
}*/